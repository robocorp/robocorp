{
  "properties": {
    "data": {
      "properties": {
        "name": {
          "default": "John",
          "description": "User's name",
          "title": "Name",
          "type": "string"
        },
        "age": {
          "default": 21,
          "description": "User's age",
          "title": "Age",
          "type": "integer"
        },
        "agreed_to_terms": {
          "description": "Indicates whether the user has agreed to the terms of service",
          "title": "Agreed To Terms",
          "type": "boolean"
        },
        "birth_date": {
          "description": "The user's birth date, optional",
          "format": "date",
          "title": "Birth Date",
          "type": "string"
        },
        "profile_url": {
          "anyOf": [
            {
              "format": "uri",
              "maxLength": 2083,
              "minLength": 1,
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "description": "The URL to the user's profile picture, optional",
          "title": "Profile Url"
        }
      },
      "required": ["name", "age", "agreed_to_terms", "birth_date", "profile_url"],
      "title": "User",
      "type": "object"
    }
  },
  "type": "object",
  "required": ["data"]
}
