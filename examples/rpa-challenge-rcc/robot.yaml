tasks:
  Challenge Python:
    shell: python -m robocorp.tasks run tasks.py -t complete_rpa_challenge

condaConfigFile: conda.yaml
artifactsDir: output
ignoreFiles:
  - .gitignore
PATH:
  - .
PYTHONPATH:
  - .
  # We want the 'robocorp.tasks' and 'robocorp.robolog' to be there too.
  - ../../tasks/src
  - ../../robolog/src
