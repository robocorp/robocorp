tasks:
  Challenge Python:
    shell: python -m robo run tasks.py -t challenge

condaConfigFile: conda.yaml
artifactsDir: output
ignoreFiles:
  - .gitignore
PATH:
  - .
PYTHONPATH:
  - .
  # We want the 'robo' to be there too.
  - ../../core/src
