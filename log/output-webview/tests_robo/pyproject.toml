[tool.robo]
name = "RPA Challenge"
description = "Tests the browser output using playwright."
python = "3.9.13"

[tool.robo.dependencies]
# We always want to run with the latest version
robocorp-tasks="0.1.7"
robocorp-browser="0.2.0"

[tool.robo.dev-dependencies]
ruff="0.0.260"
black="23.3.0"

[tool.robocorp.log]
log_filter_rules = [
    {name = "playwright", kind = "log_on_project_call"},
]